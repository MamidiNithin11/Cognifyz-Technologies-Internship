<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Result</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4, #fad0c4, #ffdde1);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .container {
      background: linear-gradient(135deg, rgba(255,255,255,0.3), rgba(255,255,255,0.15));
      padding: 40px 50px;
      border-radius: 20px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.25);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      text-align: center;
      max-width: 500px;
      width: 90%;
      color: #333;
    }

    h1 {
      margin-bottom: 24px;
      font-size: 30px;
      color: #2c3e50;
    }

    p {
      font-size: 18px;
      margin: 12px 0;
    }

    strong {
      color: #0077ff;
      font-weight: bold;
    }

    a {
      display: inline-block;
      margin-top: 25px;
      padding: 10px 25px;
      text-decoration: none;
      border-radius: 8px;
      background: linear-gradient(135deg, #0077ff, #00c6ff);
      color: #fff;
      font-weight: bold;
      box-shadow: 0 4px 14px rgba(0, 118, 255, 0.4);
      transition: background 0.3s ease;
    }

    a:hover {
      background: linear-gradient(135deg, #005fca, #00aaff);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Result from Server</h1>
    <p>Hey User, we are receiving your request.</p>
    <p>Your Name: <strong><%= username %></strong></p>
    <p>Your Password: <strong><%= password %></strong></p>
    <a href="/">Go Back</a>
  </div>
</body>
</html>
